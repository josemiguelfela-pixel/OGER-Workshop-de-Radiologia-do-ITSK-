export interface Registration {
  id: number;
  name: string;
  email: string;
  whatsapp: string;
  institution: string;
  course: string;
  year: string;
  created_at: string;
}

export const GOLD = '#D4AF37';
export const ADMIN_TOKEN = 'oger-admin-2026'; // Simple token for demo
